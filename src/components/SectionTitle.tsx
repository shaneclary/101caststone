export default function SectionTitle({ children }: { children: React.ReactNode }) {
  return <h2 className="relative font-display text-[34px] md:text-[40px] tracking-[-0.01em] text-basalt etched">{children}</h2>;
}
